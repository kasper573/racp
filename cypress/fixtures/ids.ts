// If you change these values you also need to update all rathena fixture files using the same values.
// These vars only exist to avoid spreading copies of these values across all test suites.
// (An improved solution would be to use templates for the rathena fixture files, so they can derive from these vars)
export const testItemId = 123456789;
export const testMonsterId = 32767;
export const testMapId = "test_map";
export const testSkillId = 9999999;
